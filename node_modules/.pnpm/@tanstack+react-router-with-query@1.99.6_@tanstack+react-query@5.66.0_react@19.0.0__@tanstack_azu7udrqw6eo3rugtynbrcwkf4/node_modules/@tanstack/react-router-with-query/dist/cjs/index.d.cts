import { AnyRouter } from '@tanstack/react-router';
import { QueryClient } from '@tanstack/react-query';
export declare function routerWithQueryClient<TRouter extends AnyRouter>(router: TRouter['options']['context'] extends {
    queryClient: QueryClient;
} ? TRouter : never, queryClient: QueryClient): TRouter;
